<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>从大模型到 Agent：AI 应用的终极指南</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;700&family=Noto+Sans+SC:wght@300;400;500;700;900&display=swap" rel="stylesheet">
<style>
*,*::before,*::after{margin:0;padding:0;box-sizing:border-box}
:root{
  --bg:#0a0a0a;--c1:#141414;--bd:#222;--t:#f0ede8;--td:#999;--tm:#555;
  --g:#22c55e;--gd:rgba(34,197,94,.12);--o:#f97316;--od:rgba(249,115,22,.12);
  --m:'JetBrains Mono',monospace;--s:'Noto Sans SC',-apple-system,sans-serif
}
html{scroll-behavior:smooth}
body{font-family:var(--s);background:var(--bg);color:var(--t);line-height:1.8;overflow-x:hidden;-webkit-font-smoothing:antialiased}
::selection{background:var(--g);color:var(--bg)}
a{color:var(--g);text-decoration:none;transition:color .3s}a:hover{color:var(--o)}
.ctn{max-width:1200px;margin:0 auto;padding:0 24px}
section{padding:120px 0;position:relative;border-top:1px solid var(--bd)}
section:first-of-type{border-top:none}
@media(max-width:768px){section{padding:80px 0}}

.rv{opacity:0;transform:translateY(50px);transition:all .8s cubic-bezier(.16,1,.3,1)}
.rv.v{opacity:1;transform:translateY(0)}
.rvl{opacity:0;transform:translateX(-50px);transition:all .8s cubic-bezier(.16,1,.3,1)}
.rvl.v{opacity:1;transform:translateX(0)}
.rvr{opacity:0;transform:translateX(50px);transition:all .8s cubic-bezier(.16,1,.3,1)}
.rvr.v{opacity:1;transform:translateX(0)}
.rvs{opacity:0;transform:scale(.92);transition:all .8s cubic-bezier(.16,1,.3,1)}
.rvs.v{opacity:1;transform:scale(1)}
.d1{transition-delay:.1s}.d2{transition-delay:.2s}.d3{transition-delay:.3s}
.d4{transition-delay:.4s}.d5{transition-delay:.5s}.d6{transition-delay:.6s}

nav{position:fixed;top:0;left:0;right:0;z-index:1000;padding:16px 24px;
  background:rgba(10,10,10,.8);backdrop-filter:blur(20px);-webkit-backdrop-filter:blur(20px);
  border-bottom:1px solid rgba(255,255,255,.05);transition:padding .3s}
nav.scrolled{padding:10px 24px}
.ni{max-width:1200px;margin:0 auto;display:flex;justify-content:space-between;align-items:center}
.nl{font-weight:700;font-size:18px;color:var(--t);display:flex;align-items:center;gap:8px}
.nl .dot{width:8px;height:8px;background:var(--g);border-radius:50%;animation:pu 2s infinite}
@keyframes pu{0%,100%{opacity:1;transform:scale(1)}50%{opacity:.5;transform:scale(1.3)}}
.nk{display:flex;gap:28px;list-style:none}
.nk a{color:var(--td);font-size:14px;transition:color .3s}.nk a:hover{color:var(--t)}
.nt{display:none;background:none;border:none;color:var(--t);font-size:24px;cursor:pointer}
@media(max-width:768px){
  .nk{display:none;position:absolute;top:100%;left:0;right:0;background:rgba(10,10,10,.95);
    backdrop-filter:blur(20px);flex-direction:column;padding:24px;gap:16px;border-bottom:1px solid var(--bd)}
  .nk.open{display:flex}.nt{display:block}
}

#hero{min-height:100vh;display:flex;align-items:center;justify-content:center;text-align:center;
  padding-top:60px;position:relative;overflow:hidden;border-top:none}
.hbg{position:absolute;inset:0;
  background:radial-gradient(ellipse 80% 50% at 50% -20%,rgba(34,197,94,.07),transparent),
  radial-gradient(ellipse 60% 40% at 80% 80%,rgba(249,115,22,.04),transparent);pointer-events:none}
.hgr{position:absolute;inset:0;
  background-image:linear-gradient(rgba(255,255,255,.015) 1px,transparent 1px),
  linear-gradient(90deg,rgba(255,255,255,.015) 1px,transparent 1px);background-size:60px 60px;
  mask-image:radial-gradient(ellipse 70% 60% at 50% 50%,#000,transparent);
  -webkit-mask-image:radial-gradient(ellipse 70% 60% at 50% 50%,#000,transparent);pointer-events:none}
.hc{position:relative;z-index:1;padding:0 24px}
.hb{display:inline-flex;align-items:center;gap:8px;padding:8px 20px;border:1px solid var(--bd);
  border-radius:100px;font-size:13px;color:var(--td);margin-bottom:40px;animation:fu 1s ease both}
.hb .lv{width:6px;height:6px;background:var(--g);border-radius:50%;animation:pu 2s infinite}
.ht{font-size:clamp(32px,6vw,72px);font-weight:900;line-height:1.15;letter-spacing:-.02em;
  margin-bottom:24px;animation:fu 1s .2s ease both}
.g{color:var(--g)}.o{color:var(--o)}
.hs{font-size:clamp(16px,2.5vw,22px);color:var(--td);font-weight:300;max-width:600px;
  margin:0 auto 60px;animation:fu 1s .4s ease both}
.si{animation:fu 1s .8s ease both}
.mo{width:26px;height:42px;border:2px solid var(--tm);border-radius:13px;margin:0 auto 10px;position:relative}
.wh{width:4px;height:10px;background:var(--td);border-radius:2px;position:absolute;top:8px;
  left:50%;transform:translateX(-50%);animation:sw 2s infinite}
@keyframes sw{0%{opacity:1;transform:translateX(-50%) translateY(0)}100%{opacity:0;transform:translateX(-50%) translateY(12px)}}
@keyframes fu{from{opacity:0;transform:translateY(30px)}to{opacity:1;transform:translateY(0)}}
.stx{font-size:12px;color:var(--tm);letter-spacing:2px}

.sl{display:inline-flex;align-items:center;gap:8px;font-family:var(--m);font-size:13px;
  color:var(--g);letter-spacing:3px;margin-bottom:20px;text-transform:uppercase}
.sl::before{content:'';width:24px;height:1px;background:var(--g)}
.stt{font-size:clamp(28px,5vw,52px);font-weight:900;line-height:1.2;letter-spacing:-.02em;margin-bottom:16px}
.sd{font-size:18px;color:var(--td);font-weight:300;max-width:640px;margin-bottom:48px}

.mp{background:var(--c1);border:1px solid var(--bd);border-radius:16px;padding:40px;
  margin-bottom:48px;position:relative;overflow:hidden}
.mp::before{content:'';position:absolute;top:0;left:0;right:0;height:1px;
  background:linear-gradient(90deg,transparent,var(--g),transparent)}
.mp .em{font-size:48px;margin-bottom:16px}
.mp h3{font-size:22px;font-weight:700;margin-bottom:12px}
.mp p{font-size:16px;color:var(--td);line-height:2}

.sg{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:20px;margin:48px 0}
.sc{background:var(--c1);border:1px solid var(--bd);border-radius:16px;padding:28px;text-align:center;transition:all .3s}
.sc:hover{border-color:var(--g);transform:translateY(-4px)}
.sn{font-family:var(--m);font-size:40px;font-weight:700;color:var(--g)}.sn.o{color:var(--o)}
.su{font-size:13px;color:var(--td);margin-bottom:6px}
.slb{font-size:15px;font-weight:500}

.cg{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:20px;margin:48px 0}
.cc{background:var(--c1);border:1px solid var(--bd);border-radius:16px;padding:28px;transition:border-color .3s}
.cc:hover{border-color:var(--tm)}
.ci2{font-size:28px;margin-bottom:12px}
.cn2{font-size:16px;font-weight:700;margin-bottom:8px;font-family:var(--m);color:var(--g)}
.cc p{font-size:14px;color:var(--td);line-height:1.9}

.fl{display:flex;align-items:center;justify-content:center;gap:0;margin:48px 0;flex-wrap:wrap}
.fb{background:var(--c1);border:1px solid var(--bd);border-radius:12px;padding:20px 24px;text-align:center;min-width:120px;transition:all .3s}
.fb:hover{border-color:var(--g);transform:translateY(-2px)}
.fb .ic{font-size:26px;margin-bottom:6px}.fb .lb{font-size:14px;font-weight:500}
.fb .sb{font-size:11px;color:var(--td);margin-top:4px}
.fa{font-size:22px;color:var(--g);padding:0 10px}
@media(max-width:640px){.fl{flex-direction:column}.fa{transform:rotate(90deg);padding:6px 0}}

.tb{background:var(--c1);border:1px solid var(--bd);border-radius:16px;padding:36px;margin-top:48px;position:relative}
.tb::before{content:'🔬 技术深度';position:absolute;top:-14px;left:28px;background:var(--bg);
  padding:0 12px;font-size:13px;font-weight:500;color:var(--o);font-family:var(--m)}
.tb h4{font-size:17px;margin-bottom:12px;color:var(--o)}
.tb p{font-size:14px;color:var(--td);line-height:2;margin-bottom:14px}.tb p:last-child{margin-bottom:0}

.cd{background:#111;border:1px solid var(--bd);border-radius:8px;padding:20px;margin:16px 0;
  font-family:var(--m);font-size:13px;line-height:1.9;color:var(--td);overflow-x:auto;white-space:pre}
.kw{color:var(--o)}.str{color:var(--g)}.cmt{color:var(--tm)}.fn{color:#60a5fa}

.tr{display:flex;gap:14px;margin:24px 0;flex-wrap:wrap}
.ts2{flex:1;min-width:140px;padding:20px;background:#111;border-radius:12px;border:1px solid var(--bd);text-align:center}
.ts2 .nm{font-family:var(--m);font-size:12px;color:var(--o);margin-bottom:6px}
.ts2 .na{font-weight:700;font-size:14px;margin-bottom:6px}
.ts2 .de{font-size:12px;color:var(--td)}

.rf{display:flex;align-items:stretch;justify-content:center;gap:0;margin:40px 0;flex-wrap:wrap}
.rs{background:var(--c1);border:1px solid var(--bd);border-radius:16px;padding:24px 20px;text-align:center;flex:1;min-width:140px;max-width:200px;transition:all .3s}
.rs:hover{border-color:var(--g)}
.rs .nm{font-family:var(--m);font-size:11px;color:var(--g);margin-bottom:6px}
.rs .ic{font-size:28px;margin-bottom:8px}.rs .ti{font-size:14px;font-weight:700;margin-bottom:6px}
.rs .de{font-size:12px;color:var(--td)}
.ra{display:flex;align-items:center;font-size:22px;color:var(--g);padding:0 6px}
@media(max-width:768px){.rf{flex-direction:column;align-items:center}.rs{max-width:100%;width:100%}.ra{transform:rotate(90deg);padding:4px 0}}

.rw{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:16px;margin-top:32px}
.rwc{background:var(--c1);border:1px solid var(--bd);border-radius:12px;padding:24px}
.rwc .ic{font-size:24px;margin-bottom:10px}.rwc .ti{font-weight:700;font-size:15px;margin-bottom:6px}
.rwc .de{font-size:13px;color:var(--td)}

.ai2{font-size:18px;color:var(--td);line-height:2;margin-bottom:48px;max-width:720px}.ai2 strong{color:var(--t)}

.lw{display:flex;justify-content:center;margin:48px 0}
.lo{display:grid;grid-template-columns:1fr 1fr;gap:16px;max-width:560px;width:100%;position:relative}
.lo::after{content:'🔄';position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);font-size:32px;animation:spn 8s linear infinite}
@keyframes spn{from{transform:translate(-50%,-50%) rotate(0)}to{transform:translate(-50%,-50%) rotate(360deg)}}
.loi{background:var(--c1);border:1px solid var(--bd);border-radius:16px;padding:24px;text-align:center;transition:all .3s}
.loi:hover{border-color:var(--g);transform:scale(1.02)}
.loi .ic{font-size:32px;margin-bottom:10px}.loi .na{font-size:17px;font-weight:700;margin-bottom:6px}
.loi .de{font-size:13px;color:var(--td)}
@media(max-width:480px){.lo{grid-template-columns:1fr}}

.ss{margin:72px 0;padding-top:36px;border-top:1px solid var(--bd)}
.sst{font-size:clamp(20px,3.5vw,30px);font-weight:700;margin-bottom:10px;display:flex;align-items:center;gap:10px;flex-wrap:wrap}
.ssd{font-size:16px;color:var(--td);line-height:2;margin-bottom:28px;max-width:720px}

.fc{background:var(--c1);border:1px solid var(--bd);border-radius:16px;overflow:hidden;margin:28px 0}
.fch{padding:12px 20px;border-bottom:1px solid var(--bd);font-family:var(--m);font-size:12px;color:var(--td);display:flex;align-items:center;gap:8px}
.fch .d{width:10px;height:10px;border-radius:50%}.fch .d1{background:#ef4444}.fch .d2{background:#eab308}.fch .d3{background:var(--g)}
.fcy{padding:20px;font-family:var(--m);font-size:13px;line-height:2.2}
.fl2{margin-bottom:4px}.fl2 .ro{color:var(--o);font-weight:700}.fl2 .ac{color:var(--g)}.fl2 .re{color:#60a5fa}

.ma{background:var(--c1);border:1px solid var(--bd);border-radius:16px;padding:32px;margin:32px 0}
.mat{font-size:15px;font-weight:700;margin-bottom:20px;color:var(--o);font-family:var(--m)}
.ml2{display:flex;align-items:center;gap:14px;padding:14px 18px;background:#111;border-radius:10px;border:1px solid var(--bd);transition:all .3s;margin-bottom:8px}
.ml2:hover{border-color:var(--g)}
.ml2 .mi{font-size:26px;min-width:36px;text-align:center}.ml2 .mn{font-family:var(--m);font-weight:700;font-size:13px;color:var(--g);min-width:110px}
.ml2 .md2{font-size:13px;color:var(--td)}
.maw{text-align:center;color:var(--tm);font-size:16px;padding:4px 0}

.m3{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin:28px 0}
@media(max-width:640px){.m3{grid-template-columns:1fr}}
.mc{background:var(--c1);border:1px solid var(--bd);border-radius:16px;padding:24px;text-align:center;transition:all .3s}
.mc:hover{border-color:var(--g);transform:translateY(-4px)}
.mc .mci{font-size:32px;margin-bottom:12px}.mc .mcn{font-family:var(--m);font-size:15px;font-weight:700;margin-bottom:4px;color:var(--g)}
.mc .mce{font-family:var(--m);font-size:11px;color:var(--tm);margin-bottom:10px}
.mc .mcd{font-size:13px;color:var(--td);line-height:1.8}

.me{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:14px;margin:28px 0}
.mex{background:#111;border:1px solid var(--bd);border-radius:12px;padding:20px;text-align:center;transition:all .3s}
.mex:hover{border-color:var(--g)}
.mex .ei{font-size:28px;margin-bottom:8px}.mex .en{font-size:14px;font-weight:700;margin-bottom:4px}
.mex .ed{font-size:12px;color:var(--td)}

.skc{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin:28px 0}
@media(max-width:640px){.skc{grid-template-columns:1fr}}
.sks{background:var(--c1);border:1px solid var(--bd);border-radius:16px;padding:28px}
.sks h4{font-size:16px;font-weight:700;margin-bottom:12px;font-family:var(--m)}
.sks h4.gt{color:var(--g)}.sks h4.ot{color:var(--o)}
.sks ul{list-style:none;padding:0}
.sks li{font-size:14px;color:var(--td);padding:6px 0;border-bottom:1px solid rgba(255,255,255,.03)}
.sks li::before{content:'→ ';color:var(--g)}.sks li:last-child{border-bottom:none}

.mg{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin:28px 0}
@media(max-width:640px){.mg{grid-template-columns:1fr}}
.mgc{background:var(--c1);border:1px solid var(--bd);border-radius:16px;padding:28px;transition:all .3s}
.mgc:hover{border-color:var(--g)}
.mgc .mgi{font-size:32px;margin-bottom:12px}.mgc h4{font-size:17px;font-weight:700;margin-bottom:8px}
.mgc p{font-size:14px;color:var(--td);line-height:1.9}

.pd{display:grid;grid-template-columns:repeat(auto-fit,minmax(340px,1fr));gap:20px;margin:48px 0}
@media(max-width:480px){.pd{grid-template-columns:1fr}}
.pc{background:var(--c1);border:1px solid var(--bd);border-radius:16px;padding:32px;transition:all .4s;position:relative;overflow:hidden}
.pc::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:var(--g);opacity:0;transition:opacity .3s}
.pc:hover{border-color:var(--g);transform:translateY(-6px)}.pc:hover::before{opacity:1}
.ph2{display:flex;align-items:center;gap:14px;margin-bottom:16px}
.pi2{font-size:36px}.pn2{font-size:20px;font-weight:700}.pp2{font-size:13px;color:var(--td)}
.pf{list-style:none;margin:12px 0;padding:0}
.pf li{font-size:14px;color:var(--td);padding:4px 0;display:flex;align-items:flex-start;gap:8px}
.pf li::before{content:'✦';color:var(--g);font-size:10px;margin-top:5px;flex-shrink:0}
.phl{background:var(--gd);border:1px solid rgba(34,197,94,.2);border-radius:8px;padding:12px 16px;margin:12px 0;font-size:13px;color:var(--g)}
.phl strong{color:var(--t)}
.pm2{display:flex;justify-content:space-between;align-items:center;margin-top:16px;padding-top:12px;border-top:1px solid var(--bd)}
.pmf{font-size:12px;color:var(--td)}.pml{font-family:var(--m);font-size:12px;color:var(--g)}

.tw{overflow-x:auto;margin:48px 0;border-radius:16px;border:1px solid var(--bd)}
.ct{width:100%;border-collapse:collapse;font-size:14px;min-width:700px}
.ct th{background:#111;padding:16px 14px;text-align:left;font-weight:600;font-size:13px;border-bottom:1px solid var(--bd);white-space:nowrap}
.ct th:first-child{color:var(--td)}
.ct td{padding:14px;border-bottom:1px solid rgba(255,255,255,.03)}.ct tr:last-child td{border-bottom:none}
.ct td:first-child{font-weight:500;color:var(--td);font-size:13px;white-space:nowrap}
.ct .y{color:var(--g)}.ct .n{color:#ef4444}.ct tr:hover td{background:rgba(255,255,255,.02)}

.ftc{display:grid;grid-template-columns:repeat(auto-fit,minmax(300px,1fr));gap:20px;margin:48px 0}
.ftd{background:var(--c1);border:1px solid var(--bd);border-radius:16px;padding:32px;transition:all .3s}
.ftd:hover{border-color:var(--g);transform:translateY(-4px)}
.ftd .fi{font-size:36px;margin-bottom:16px}.ftd h3{font-size:20px;font-weight:700;margin-bottom:10px}
.ftd p{font-size:15px;color:var(--td);line-height:1.9}

footer{padding:60px 0;border-top:1px solid var(--bd);text-align:center}
.fp{font-size:14px;color:var(--td);margin-bottom:16px;display:flex;align-items:center;justify-content:center;gap:8px}
.fp .dot{width:6px;height:6px;background:var(--g);border-radius:50%;animation:pu 2s infinite}
.fls{display:flex;justify-content:center;gap:24px;margin-bottom:16px}
.fls a{font-size:13px;color:var(--tm)}.fls a:hover{color:var(--g)}
.fcp{font-size:12px;color:var(--tm)}
.hl{color:var(--g);font-weight:500}.hlo{color:var(--o);font-weight:500}
</style>
</head>
<body>
<!-- NAV -->
<nav id="nav">
<div class="ni">
<div class="nl"><span class="dot"></span> AI Agent 指南</div>
<button class="nt" onclick="document.querySelector('.nk').classList.toggle('open')" aria-label="菜单">☰</button>
<ul class="nk">
<li><a href="#llm" onclick="cn()">大模型</a></li>
<li><a href="#rag" onclick="cn()">RAG</a></li>
<li><a href="#agent" onclick="cn()">Agent</a></li>
<li><a href="#products" onclick="cn()">产品</a></li>
<li><a href="#compare" onclick="cn()">对比</a></li>
<li><a href="#future" onclick="cn()">未来</a></li>
</ul>
</div>
</nav>

<!-- HERO -->
<section id="hero">
<div class="hbg"></div>
<div class="hgr"></div>
<div class="hc">
<div class="hb"><span class="lv"></span> 2025 · 全面解读</div>
<h1 class="ht">从大模型到 <span class="g">Agent</span><br>AI 应用的<span class="o">终极指南</span></h1>
<p class="hs">写给所有人的 AI 进化史 — 从能聊天到能干活</p>
<div class="si">
<div class="mo"><div class="wh"></div></div>
<div class="stx">向下滚动</div>
</div>
</div>
</section>
<!-- LLM SECTION -->
<section id="llm">
<div class="ctn">

<div class="rv">
<div class="sl">Section 01</div>
<h2 class="stt">大模型基础 —「AI 的大脑」</h2>
<p class="sd">在理解 Agent 之前，我们先认识它的核心引擎：大语言模型（LLM）。</p>
</div>

<!-- Metaphor -->
<div class="mp rv">
<div class="em">🧠</div>
<h3>把大模型想象成一个「读了全世界所有书的超级学霸」</h3>
<p>他读过维基百科的每一篇文章、GitHub 上的每一行代码、Reddit 上的每一条帖子、豆瓣上的每一篇书评。他的知识面广到不可思议——上知天文下知地理，能写诗也能写代码。但他有个特点：<strong style="color:var(--t)">他不会上网搜索，只能凭记忆回答</strong>。你问他什么，他就从记忆里找最合理的答案拼给你。有时候记忆模糊了，他甚至会自信满满地「编」一个听起来很对的答案——这就是我们说的「幻觉」(Hallucination)。</p>
</div>

<!-- Core Concepts -->
<div class="cg">
<div class="cc rv d1">
<div class="ci2">🔤</div>
<div class="cn2">Token（词元）</div>
<p>大模型不是逐字阅读的，它把文本切成一个个小块——Token。一个汉字通常是 1-2 个 Token，一个英文单词是 1-3 个 Token。"你好世界" ≈ 4 个 Token。模型的计费、速度、上下文长度，都以 Token 为单位衡量。你可以把 Token 理解为 AI 的「阅读单位」。</p>
</div>
<div class="cc rv d2">
<div class="ci2">📏</div>
<div class="cn2">上下文窗口（Context Window）</div>
<p>这是模型一次能「看到」多少内容。就像人的工作记忆——你一次能同时记住多少东西。GPT-4 Turbo 有 128K Token 的窗口（约 10 万字），Claude 3 最高 200K Token。窗口越大，模型能处理的文本越长——可以一次读完一本小说，或者分析整个代码库。</p>
</div>
<div class="cc rv d3">
<div class="ci2">⚡</div>
<div class="cn2">推理（Inference）</div>
<p>当你向 AI 提问时，模型做的事情叫「推理」。它不是从数据库里查答案，而是根据你的输入，逐词预测最可能的下一个词。就像补全句子——"今天天气真"→"好"。这个过程每秒计算数十亿次浮点运算，需要强大的 GPU 算力支撑。</p>
</div>
</div>

<!-- Stats -->
<div class="sg">
<div class="sc rv d1">
<div class="sn" data-target="18000" data-suffix="亿">0</div>
<div class="su">参数量</div>
<div class="slb">GPT-4</div>
</div>
<div class="sc rv d2">
<div class="sn o" data-target="200" data-suffix="K">0</div>
<div class="su">上下文窗口</div>
<div class="slb">Claude 3</div>
</div>
<div class="sc rv d3">
<div class="sn" data-target="15" data-suffix="万亿">0</div>
<div class="su">训练数据 Token 数</div>
<div class="slb">Llama 3</div>
</div>
<div class="sc rv d4">
<div class="sn o" data-target="100" data-suffix="+">0</div>
<div class="su">支持语言数</div>
<div class="slb">GPT-4o</div>
</div>
</div>

<!-- Flow: Input → Model → Output -->
<h3 class="rv" style="font-size:20px;margin-bottom:8px;">大模型的工作原理（极简版）</h3>
<p class="rv" style="color:var(--td);margin-bottom:16px;">本质上，大模型就是一个超级复杂的「文本接龙」机器：</p>
<div class="fl rv">
<div class="fb">
<div class="ic">💬</div>
<div class="lb">输入（Prompt）</div>
<div class="sb">你的问题/指令</div>
</div>
<div class="fa">→</div>
<div class="fb">
<div class="ic">🧠</div>
<div class="lb">模型推理</div>
<div class="sb">数十亿参数计算</div>
</div>
<div class="fa">→</div>
<div class="fb">
<div class="ic">📝</div>
<div class="lb">输出（Response）</div>
<div class="sb">逐词生成回答</div>
</div>
</div>

<!-- Tech Deep Dive -->
<div class="tb rv">
<h4>Transformer 架构：大模型的基石</h4>
<p>2017 年，Google 发表了划时代的论文《Attention Is All You Need》，提出了 Transformer 架构。它的核心创新是<strong style="color:var(--t)">自注意力机制（Self-Attention）</strong>——让模型在处理每个词时，能「关注」到输入中其他所有词的信息，而不是像传统 RNN 那样只能顺序处理。</p>
<p>举个例子：在句子"小明把苹果给了小红，因为她饿了"中，模型需要理解"她"指的是"小红"。自注意力机制让模型能直接建立"她"和"小红"之间的关联，不管它们隔了多远。</p>

<h4 style="margin-top:24px;">训练三部曲：从白纸到专家</h4>
<div class="tr">
<div class="ts2">
<div class="nm">STEP 1</div>
<div class="na">预训练</div>
<div class="de">用海量文本（万亿 Token）训练模型预测下一个词。这一步让模型获得广泛的世界知识。耗时数月，花费数千万美元。</div>
</div>
<div class="ts2">
<div class="nm">STEP 2</div>
<div class="na">指令微调（SFT）</div>
<div class="de">用精心标注的"问答对"教模型学会按指令回答问题。从"语言模型"变成"助手"。</div>
</div>
<div class="ts2">
<div class="nm">STEP 3</div>
<div class="na">RLHF</div>
<div class="de">基于人类反馈的强化学习。让人类评估回答质量，模型据此优化，学会生成人类偏好的回答。</div>
</div>
</div>
</div>

</div>
</section>
<!-- RAG SECTION -->
<section id="rag">
<div class="ctn">

<div class="rv">
<div class="sl">Section 02</div>
<h2 class="stt">RAG —「给 AI 配一个图书馆」</h2>
<p class="sd">让 AI 查资料再回答，而不是全靠记忆。</p>
</div>

<p class="rv ai2">RAG（Retrieval-Augmented Generation，检索增强生成）的核心思想很简单：<strong>与其让 AI 死记硬背所有知识，不如在回答前先让它「查资料」</strong>。就像一个聪明的学生在考试中被允许翻书——他不需要背下所有内容，只需要知道去哪找、怎么用。</p>

<!-- RAG Flow -->
<div class="rf rv">
<div class="rs">
<div class="nm">STEP 1</div>
<div class="ic">❓</div>
<div class="ti">用户提问</div>
<div class="de">"公司最新的报销政策是什么？"</div>
</div>
<div class="ra">→</div>
<div class="rs">
<div class="nm">STEP 2</div>
<div class="ic">🔍</div>
<div class="ti">检索知识库</div>
<div class="de">从文档库中找到最相关的内容片段</div>
</div>
<div class="ra">→</div>
<div class="rs">
<div class="nm">STEP 3</div>
<div class="ic">🧩</div>
<div class="ti">组合上下文</div>
<div class="de">将检索结果 + 原始问题拼接成提示词</div>
</div>
<div class="ra">→</div>
<div class="rs">
<div class="nm">STEP 4</div>
<div class="ic">✨</div>
<div class="ti">生成回答</div>
<div class="de">基于真实资料生成准确、可溯源的回答</div>
</div>
</div>

<!-- Why RAG -->
<div class="rw rv">
<div class="rwc">
<div class="ic">📅</div>
<div class="ti">知识有截止日期</div>
<div class="de">模型的训练数据有时间截止点。2024 年初训练的模型不知道 2024 年底发生了什么。RAG 让 AI 实时获取最新信息。</div>
</div>
<div class="rwc">
<div class="ic">🌀</div>
<div class="ti">减少幻觉</div>
<div class="de">基于检索到的真实文档回答，而非凭空编造，大幅降低"胡说八道"的概率，并且回答可溯源。</div>
</div>
<div class="rwc">
<div class="ic">🏢</div>
<div class="ti">企业私有知识</div>
<div class="de">公司内部文档、产品手册、客户资料——这些不在互联网上，模型没见过。RAG 让 AI 能使用你的私有数据。</div>
</div>
</div>

</div>
</section>
<!-- AGENT SECTION -->
<section id="agent">
<div class="ctn">

<div class="rv">
<div class="sl">Section 03</div>
<h2 class="stt">Agent —「AI 有了手和脚」</h2>
<p class="sd">从"能聊天"到"能干活"的跃迁。这是 AI 应用最激动人心的前沿。</p>
</div>

<div class="ai2 rv">
<p>如果说大模型是「大脑」，RAG 是「图书馆」，那么 <strong>Agent 就是让 AI 拥有了「手」和「脚」</strong>。</p>
<p style="margin-top:16px;">一个普通的聊天机器人只能跟你对话——你问它天气，它只能说"我无法查看实时天气"。但一个 Agent 不同：你问它天气，它会<strong>自己调用天气 API 查询</strong>，然后告诉你结果；你让它订机票，它会<strong>自己打开浏览器搜索、比价、填写表单</strong>。Agent 不只是「说」，它还能「做」。</p>
</div>

<!-- Agent Loop -->
<h3 class="rv" style="font-size:22px;margin-bottom:24px;">Agent 的核心循环</h3>
<div class="lw rv">
<div class="lo">
<div class="loi d1">
<div class="ic">👁️</div>
<div class="na">感知 Perceive</div>
<div class="de">接收用户指令、环境信息、工具返回结果</div>
</div>
<div class="loi d2">
<div class="ic">🎯</div>
<div class="na">规划 Plan</div>
<div class="de">分析任务，分解步骤，制定执行策略</div>
</div>
<div class="loi d3">
<div class="ic">🔧</div>
<div class="na">执行 Act</div>
<div class="de">调用工具、运行代码、操作外部系统</div>
</div>
<div class="loi d4">
<div class="ic">📊</div>
<div class="na">反馈 Reflect</div>
<div class="de">评估结果，决定是否需要调整并继续</div>
</div>
</div>
</div>

<!-- Function Calling -->
<div class="ss rv">
<h3 class="sst">🔌 工具调用（Function Calling）</h3>
<p class="ssd">Agent 之所以能"干活"，核心秘密就是<strong style="color:var(--t)">工具调用</strong>。大模型本身不能上网、不能读文件、不能操作数据库——但我们可以给它一份「工具菜单」，告诉它有哪些工具可用、每个工具的功能和参数。当模型判断需要使用某个工具时，它会输出一段结构化的调用请求，由外部系统执行后将结果返回给模型。</p>

<div class="fc rv">
<div class="fch">
<span class="d d1"></span><span class="d d2"></span><span class="d d3"></span>
<span style="margin-left:8px">function-calling-demo.txt</span>
</div>
<div class="fcy">
<div class="fl2"><span class="ro">用户：</span> 帮我查一下北京今天的天气</div>
<div class="fl2"><span class="ro">Agent 思考：</span> <span style="color:var(--tm)">用户想知道北京天气，我需要调用天气查询工具</span></div>
<div class="fl2"><span class="ac">→ 调用工具：</span> get_weather(city="北京", date="today")</div>
<div class="fl2"><span class="re">← 工具返回：</span> {"temp": "22°C", "weather": "晴", "wind": "北风3级"}</div>
<div class="fl2"><span class="ro">Agent：</span> 北京今天天气晴朗，气温 22°C，北风 3 级，适合外出！</div>
</div>
</div>
</div>

<!-- MCP -->
<div class="ss rv">
<h3 class="sst">🔗 MCP（Model Context Protocol）详解</h3>
<p class="ssd">由 Anthropic 提出的开放协议，<strong style="color:var(--t)">让 AI 统一调用外部工具和数据源</strong>。如果说 Function Calling 是让 AI 能用工具，MCP 就是为这些工具定义了一套「通用接口标准」。</p>

<div class="mp rv" style="margin-bottom:32px">
<div class="em">🔌</div>
<h3>类比：MCP 就是 AI 世界的「USB 接口」</h3>
<p>在 USB 出现之前，每个外设都有自己独特的接口——打印机一种、键盘一种、鼠标又一种。USB 统一了这一切。MCP 做的是同样的事：<strong style="color:var(--t)">在 MCP 之前，每个 AI 应用要对接不同工具，都要写不同的代码；有了 MCP，所有工具遵循同一套协议</strong>，AI 应用只需要实现一次 MCP 客户端，就能连接任何 MCP 服务器提供的工具。</p>
</div>

<!-- MCP Architecture -->
<div class="ma rv">
<div class="mat">MCP 架构层次</div>
<div class="ml2">
<div class="mi">🖥️</div>
<div class="mn">MCP Host</div>
<div class="md2">AI 应用本身（如 Claude Desktop、VS Code、OpenClaw），是用户直接交互的界面</div>
</div>
<div class="maw">↕</div>
<div class="ml2">
<div class="mi">🔄</div>
<div class="mn">MCP Client</div>
<div class="md2">协议客户端，负责与 MCP Server 建立连接、发送请求、接收响应</div>
</div>
<div class="maw">↕</div>
<div class="ml2">
<div class="mi">⚙️</div>
<div class="mn">MCP Server</div>
<div class="md2">工具提供方，将具体能力（API、数据库、文件系统等）封装成标准化的 MCP 服务</div>
</div>
<div class="maw">↕</div>
<div class="ml2">
<div class="mi">🌐</div>
<div class="mn">Tools/Resources</div>
<div class="md2">实际的外部资源——GitHub API、PostgreSQL 数据库、本地文件系统、Slack 等</div>
</div>
</div>

<!-- MCP 3 Core -->
<h4 class="rv" style="font-size:18px;margin:32px 0 8px;">MCP 的三大核心概念</h4>
<div class="m3 rv">
<div class="mc">
<div class="mci">🔧</div>
<div class="mcn">Tools</div>
<div class="mce">工具</div>
<div class="mcd">可被 AI 调用的函数/操作。比如"搜索文件"、"执行 SQL 查询"、"发送邮件"。每个工具都有明确的输入参数和输出格式。</div>
</div>
<div class="mc">
<div class="mci">📦</div>
<div class="mcn">Resources</div>
<div class="mce">资源</div>
<div class="mcd">AI 可以读取的数据源。比如文件内容、数据库记录、API 返回的数据。资源是只读的，提供上下文信息。</div>
</div>
<div class="mc">
<div class="mci">💡</div>
<div class="mcn">Prompts</div>
<div class="mce">提示模板</div>
<div class="mcd">预定义的交互模板。比如"代码审查"模板、"文档摘要"模板，让 AI 按特定方式处理特定类型的任务。</div>
</div>
</div>

<!-- MCP Examples -->
<h4 class="rv" style="font-size:18px;margin:32px 0 8px;">MCP 实际应用示例</h4>
<div class="me rv">
<div class="mex">
<div class="ei">🐙</div>
<div class="en">GitHub MCP</div>
<div class="ed">创建 Issue、提交 PR、搜索代码、管理仓库</div>
</div>
<div class="mex">
<div class="ei">🗄️</div>
<div class="en">数据库 MCP</div>
<div class="ed">查询 SQL、分析数据、生成报表</div>
</div>
<div class="mex">
<div class="ei">📁</div>
<div class="en">文件系统 MCP</div>
<div class="ed">读写文件、搜索目录、管理文档</div>
</div>
<div class="mex">
<div class="ei">💬</div>
<div class="en">Slack MCP</div>
<div class="ed">发消息、搜索聊天记录、管理频道</div>
</div>
</div>
</div>

<!-- Skills -->
<div class="ss rv">
<h3 class="sst">🧩 Skills（技能系统）详解</h3>
<p class="ssd">如果说 MCP 是「接口标准」，那么 Skills 是<strong style="color:var(--t)">更高层的能力封装</strong>。一个 Skill 不仅包含工具调用，还包含指令、脚本、资源文件和上下文知识——它是一个完整的「能力包」。</p>

<div class="skc rv">
<div class="sks">
<h4 class="gt">MCP（底层协议）</h4>
<ul>
<li>定义工具的调用接口标准</li>
<li>关注"怎么连接"</li>
<li>类比：USB 协议规范</li>
<li>开发者需要理解协议细节</li>
<li>单个工具粒度</li>
</ul>
</div>
<div class="sks">
<h4 class="ot">Skills（能力封装）</h4>
<ul>
<li>包含指令 + 脚本 + 工具 + 资源</li>
<li>关注"能做什么"</li>
<li>类比：App Store 里的应用</li>
<li>用户只需安装、即插即用</li>
<li>完整能力包，组合多个工具</li>
</ul>
</div>
</div>

<div class="mp rv" style="margin-top:28px">
<div class="em">🏪</div>
<h3>Skills 就像 AI 的「App Store」</h3>
<p>想让你的 Agent 学会分析股票？安装一个「股票分析」技能——它包含了 K 线图绘制脚本、技术指标计算工具、财报分析提示词模板，以及如何解读市场数据的指令。安装完成后，你只需对 Agent 说"帮我分析一下茅台最近的走势"，它就知道该怎么做了。不需要你手动配置 API、写提示词、调参数——<strong style="color:var(--t)">技能已经把这一切打包好了</strong>。</p>
</div>
</div>

<!-- Memory -->
<div class="ss rv">
<h3 class="sst">🧠 Agent 的记忆系统</h3>
<p class="ssd">Agent 不只是一次性的"问-答"。它需要记住你是谁、你的偏好、之前的对话，才能像一个真正的助手一样工作。</p>

<div class="mg rv">
<div class="mgc">
<div class="mgi">💭</div>
<h4>短期记忆（工作记忆）</h4>
<p>就是当前对话的上下文窗口。Agent 记得你这轮对话中说过的每句话。但关闭对话后，这些记忆就会消失——就像人的工作记忆，你挂了电话就忘了刚才的号码。受限于上下文窗口大小。</p>
</div>
<div class="mgc">
<div class="mgi">📚</div>
<h4>长期记忆（持久化存储）</h4>
<p>通过文件系统、数据库或向量存储来持久保存信息。Agent 可以记住你的工作习惯、项目进展、个人偏好。即使关闭会话，下次对话时它仍然"记得"你。这是让 Agent 真正个性化的关键。</p>
</div>
</div>
</div>

<!-- Multi-Agent -->
<div class="ss rv">
<h3 class="sst">🤝 多 Agent 协作</h3>
<p class="ssd">一个人干不了所有事，一个 Agent 也是。复杂任务需要多个专业 Agent 分工合作——就像一个团队。</p>

<div class="mg rv">
<div class="mgc">
<div class="mgi">👨‍💻</div>
<h4>分工明确</h4>
<p>一个"程序员 Agent"负责写代码，一个"测试 Agent"负责找 Bug，一个"产品经理 Agent"负责审查需求。每个 Agent 专注于自己擅长的领域，协作完成复杂项目。</p>
</div>
<div class="mgc">
<div class="mgi">🔀</div>
<h4>编排与调度</h4>
<p>需要一个"指挥官"来协调各个 Agent：谁先干、谁后干、谁的结果传给谁。这就是 Agent 编排（Orchestration）——可以是固定流程，也可以让 AI 动态决定。</p>
</div>
</div>
</div>

</div>
</section>
<!-- PRODUCTS SECTION -->
<section id="products">
<div class="ctn">

<div class="rv">
<div class="sl">Section 04</div>
<h2 class="stt">产品图鉴 —「谁在做 Agent？」</h2>
<p class="sd">从编程助手到通用 Agent，这些产品正在定义 AI 应用的未来。</p>
</div>

<div class="pd">

<!-- Claude Code -->
<div class="pc rv d1">
<div class="ph2">
<div class="pi2">🤖</div>
<div>
<div class="pn2">Claude Code</div>
<div class="pp2">Anthropic 官方 AI 编程助手</div>
</div>
</div>
<ul class="pf">
<li>终端原生体验，在命令行中直接与 AI 协作编程</li>
<li>完整的 Agent 模式，可自主完成复杂编程任务</li>
<li>深度代码理解，能分析整个代码库的架构和逻辑</li>
<li>原生支持 MCP 协议，可连接各种开发工具</li>
</ul>
<div class="phl">💡 <strong>亮点：</strong>不只是代码补全——它能自主规划、编写、测试、调试，完成从零到一的开发任务。</div>
<div class="pm2">
<div class="pmf">👤 开发者、技术团队</div>
<div class="pml"><a href="https://claude.ai" target="_blank">claude.ai →</a></div>
</div>
</div>

<!-- Cursor -->
<div class="pc rv d2">
<div class="ph2">
<div class="pi2">✏️</div>
<div>
<div class="pn2">Cursor</div>
<div class="pp2">AI 原生代码编辑器</div>
</div>
</div>
<ul class="pf">
<li>基于 VS Code 深度改造，AI 原生设计</li>
<li>内置多模型支持（GPT-4、Claude 等），Tab 智能补全</li>
<li>Chat 对话式编程 + Composer 多文件协同编辑</li>
<li>支持 MCP 协议，可扩展工具能力</li>
</ul>
<div class="phl">💡 <strong>亮点：</strong>把 AI 深度融入编辑器体验——用起来最自然，学习成本最低。写代码时 AI 就在身边。</div>
<div class="pm2">
<div class="pmf">👤 所有开发者</div>
<div class="pml"><a href="https://cursor.sh" target="_blank">cursor.sh →</a></div>
</div>
</div>

<!-- Dify -->
<div class="pc rv d3">
<div class="ph2">
<div class="pi2">🔮</div>
<div>
<div class="pn2">Dify</div>
<div class="pp2">开源 LLM 应用开发平台</div>
</div>
</div>
<ul class="pf">
<li>可视化工作流编排，拖拽式构建 AI 应用</li>
<li>内置 RAG 引擎，支持多种文档格式</li>
<li>Agent 构建器，无代码配置智能助手</li>
<li>支持 OpenAI、Claude、本地模型等多种 LLM</li>
</ul>
<div class="phl">💡 <strong>亮点：</strong>不写代码也能搭建 AI 应用。产品经理、运营人员也能上手，15 分钟构建一个客服机器人。</div>
<div class="pm2">
<div class="pmf">👤 产品经理、运营团队</div>
<div class="pml"><a href="https://dify.ai" target="_blank">dify.ai →</a></div>
</div>
</div>

<!-- n8n -->
<div class="pc rv d4">
<div class="ph2">
<div class="pi2">⚡</div>
<div>
<div class="pn2">n8n</div>
<div class="pp2">开源工作流自动化平台</div>
</div>
</div>
<ul class="pf">
<li>400+ 预置集成节点，连接几乎所有主流服务</li>
<li>AI Agent 节点，让 AI 做工作流中的决策者</li>
<li>支持自托管，数据完全掌控在自己手中</li>
<li>可视化编排，支持条件分支、循环、错误处理</li>
</ul>
<div class="phl">💡 <strong>亮点：</strong>连接一切——邮件、飞书、数据库、API、文件。让 AI 在工作流中做智能决策，自动化你的日常工作。</div>
<div class="pm2">
<div class="pmf">👤 运营、产品、技术团队</div>
<div class="pml"><a href="https://n8n.io" target="_blank">n8n.io →</a></div>
</div>
</div>

<!-- Manus -->
<div class="pc rv d5">
<div class="ph2">
<div class="pi2">🦾</div>
<div>
<div class="pn2">Manus</div>
<div class="pp2">通用型 AI Agent</div>
</div>
</div>
<ul class="pf">
<li>能上网浏览、搜索信息、收集数据</li>
<li>能写代码、运行程序、处理文件</li>
<li>自主规划多步骤任务，不需要手把手指导</li>
<li>支持复杂的研究、分析、创作任务</li>
</ul>
<div class="phl">💡 <strong>亮点：</strong>给它一个目标，它自己规划路径并执行——像一个远程助理，你只需要说"做什么"，不需要说"怎么做"。</div>
<div class="pm2">
<div class="pmf">👤 所有需要 AI 帮忙的人</div>
<div class="pml"><a href="https://manus.im" target="_blank">manus.im →</a></div>
</div>
</div>

<!-- OpenClaw -->
<div class="pc rv d6">
<div class="ph2">
<div class="pi2">🐾</div>
<div>
<div class="pn2">OpenClaw</div>
<div class="pp2">个人 AI Agent 运行时框架</div>
</div>
</div>
<ul class="pf">
<li>多渠道支持——微信、QQ、飞书、Discord、Telegram</li>
<li>技能系统（Skills），按需安装能力，即插即用</li>
<li>原生 MCP 支持，工具生态无限扩展</li>
<li>完全本地部署，数据和隐私完全由你掌控</li>
</ul>
<div class="phl">💡 <strong>亮点：</strong>你的 Agent 运行在你自己的服务器上——真正属于你的 AI 助手。跨平台、可扩展、完全开源。</div>
<div class="pm2">
<div class="pmf">👤 极客、技术爱好者</div>
<div class="pml"><a href="https://github.com/nicepkg/openclaw" target="_blank">GitHub →</a></div>
</div>
</div>

</div>
</div>
</section>
<!-- COMPARE SECTION -->
<section id="compare">
<div class="ctn">

<div class="rv">
<div class="sl">Section 05</div>
<h2 class="stt">横向对比</h2>
<p class="sd">一张表看清楚：这些产品有什么不同？选哪个适合你？</p>
</div>

<div class="tw rv">
<table class="ct">
<thead>
<tr>
<th>维度</th>
<th>Claude Code</th>
<th>Cursor</th>
<th>Dify</th>
<th>n8n</th>
<th>Manus</th>
<th>OpenClaw</th>
</tr>
</thead>
<tbody>
<tr>
<td>类型</td>
<td>编程助手</td>
<td>AI 编辑器</td>
<td>应用平台</td>
<td>自动化平台</td>
<td>通用 Agent</td>
<td>Agent 框架</td>
</tr>
<tr>
<td>技术门槛</td>
<td>中</td>
<td>低</td>
<td>低</td>
<td>中</td>
<td>低</td>
<td>高</td>
</tr>
<tr>
<td>开源</td>
<td class="n">否</td>
<td class="n">否</td>
<td class="y">是</td>
<td class="y">是</td>
<td class="n">否</td>
<td class="y">是</td>
</tr>
<tr>
<td>MCP 支持</td>
<td class="y">✅</td>
<td class="y">✅</td>
<td class="n">❌</td>
<td class="y">✅</td>
<td class="n">❌</td>
<td class="y">✅</td>
</tr>
<tr>
<td>本地部署</td>
<td class="y">✅</td>
<td class="n">❌</td>
<td class="y">✅</td>
<td class="y">✅</td>
<td class="n">❌</td>
<td class="y">✅</td>
</tr>
<tr>
<td>主要用途</td>
<td>写代码</td>
<td>写代码</td>
<td>搭应用</td>
<td>自动化</td>
<td>做任务</td>
<td>私人助理</td>
</tr>
<tr>
<td>核心优势</td>
<td>深度代码理解</td>
<td>编辑器体验</td>
<td>零代码搭建</td>
<td>海量集成</td>
<td>自主执行</td>
<td>完全掌控</td>
</tr>
</tbody>
</table>
</div>

</div>
</section>
<!-- FUTURE SECTION -->
<section id="future">
<div class="ctn">

<div class="rv">
<div class="sl">Section 06</div>
<h2 class="stt">未来展望</h2>
<p class="sd">Agent 不只是一个技术概念，它正在重塑我们与数字世界交互的方式。</p>
</div>

<div class="ftc">

<div class="ftd rv d1">
<div class="fi">🌐</div>
<h3>每个人的数字分身</h3>
<p>想象一下：你有一个 AI Agent 完全了解你的工作习惯、知识偏好、社交风格。它帮你筛选邮件、整理日程、撰写报告、管理项目。它不只是工具，更像是你的数字化延伸——一个 7×24 小时不休息的"你"。这不是科幻，这是正在发生的未来。</p>
</div>

<div class="ftd rv d2">
<div class="fi">🤝</div>
<h3>从工具到伙伴</h3>
<p>当前的 AI 还是"工具"——你给指令，它执行。未来的 Agent 将更像"伙伴"——它理解你的目标和价值观，能在你没想到的时候主动提供帮助。"我注意到你这周压力很大，要不要我帮你推迟几个非紧急会议？"这种主动的、有同理心的协作，是 Agent 进化的方向。</p>
</div>

<div class="ftd rv d3">
<div class="fi">⚠️</div>
<h3>挑战：安全、隐私、可控</h3>
<p>Agent 越强大，风险越大。当 AI 能自主操作你的电脑、访问你的数据、代表你发消息时，安全和隐私就是头等大事。我们需要解决：谁来监督 Agent 的行为？敏感操作是否需要人类确认？数据存储在哪里？这些问题的答案，将决定我们能走多远。</p>
</div>

</div>

</div>
</section>
<!-- FOOTER -->
<footer>
<div class="ctn">
<div class="fp"><span class="dot"></span> 由 AI Agent 驱动生成</div>
<div class="fls">
<a href="https://github.com/nicepkg/openclaw" target="_blank">GitHub</a>
<a href="#hero">回到顶部</a>
<a href="mailto:contact@example.com">联系我们</a>
</div>
<div class="fcp">© 2025 AI Agent Guide. 内容仅供学习参考。</div>
</div>
</footer>

<!-- JAVASCRIPT -->
<script>
// Close mobile nav
function cn(){document.querySelector('.nk').classList.remove('open')}

// Nav scroll effect
const nav=document.getElementById('nav');
window.addEventListener('scroll',()=>{
  nav.classList.toggle('scrolled',window.scrollY>50);
});

// Intersection Observer for reveal animations
const observerOptions={threshold:0.1,rootMargin:'0px 0px -50px 0px'};
const observer=new IntersectionObserver((entries)=>{
  entries.forEach(entry=>{
    if(entry.isIntersecting){
      entry.target.classList.add('v');
      // Trigger counter animation for stat numbers
      const counters=entry.target.querySelectorAll('.sn[data-target]');
      counters.forEach(counter=>{
        if(!counter.dataset.counted){
          counter.dataset.counted='true';
          animateCounter(counter);
        }
      });
    }
  });
},observerOptions);

// Observe all reveal elements
document.querySelectorAll('.rv,.rvl,.rvr,.rvs').forEach(el=>observer.observe(el));

// Counter animation
function animateCounter(el){
  const target=parseInt(el.dataset.target);
  const suffix=el.dataset.suffix||'';
  const duration=2000;
  const start=performance.now();

  function update(now){
    const elapsed=now-start;
    const progress=Math.min(elapsed/duration,1);
    // Ease out cubic
    const eased=1-Math.pow(1-progress,3);
    const current=Math.floor(eased*target);
    el.textContent=current.toLocaleString()+suffix;
    if(progress<1)requestAnimationFrame(update);
  }
  requestAnimationFrame(update);
}

// Smooth scroll for anchor links
document.querySelectorAll('a[href^="#"]').forEach(a=>{
  a.addEventListener('click',e=>{
    e.preventDefault();
    const target=document.querySelector(a.getAttribute('href'));
    if(target){
      const offset=80;
      const top=target.getBoundingClientRect().top+window.pageYOffset-offset;
      window.scrollTo({top,behavior:'smooth'});
    }
  });
});

// Floating particles (subtle)
function createParticles(){
  const container=document.body;
  for(let i=0;i<15;i++){
    const p=document.createElement('div');
    p.style.cssText=`
      position:fixed;width:${Math.random()*3+1}px;height:${Math.random()*3+1}px;
      background:${Math.random()>.5?'var(--g)':'var(--o)'};border-radius:50%;
      pointer-events:none;opacity:0;z-index:0;
      left:${Math.random()*100}vw;top:${Math.random()*100}vh;
    `;
    container.appendChild(p);
    animateParticle(p);
  }
}

function animateParticle(p){
  const duration=Math.random()*6000+4000;
  const startX=Math.random()*100;
  const startY=100+Math.random()*10;
  p.style.left=startX+'vw';
  p.style.top=startY+'vh';

  p.animate([
    {opacity:0,transform:'translateY(0)'},
    {opacity:.3,transform:`translateY(-${30+Math.random()*40}vh)`},
    {opacity:0,transform:`translateY(-${60+Math.random()*40}vh)`}
  ],{
    duration,
    easing:'linear',
    iterations:Infinity,
    delay:Math.random()*duration
  });
}

createParticles();

// Active nav link highlight
const sections=document.querySelectorAll('section[id]');
window.addEventListener('scroll',()=>{
  let current='';
  sections.forEach(s=>{
    const top=s.offsetTop-120;
    if(window.pageYOffset>=top)current=s.getAttribute('id');
  });
  document.querySelectorAll('.nk a').forEach(a=>{
    a.style.color=a.getAttribute('href')==='#'+current?'var(--t)':'';
  });
});
</script>
</body>
</html>
